apiVersion: v1
kind: Service
metadata:
  name: orders-postgres
  namespace: service-health-dashboard
spec:
  type: ClusterIP
  selector:
    app: orders-postgres
  ports:
    - port: 5432
      targetPort: 5432
